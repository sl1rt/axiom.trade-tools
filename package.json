{
  "name": "axiom-early-wallets",
  "version": "0.3.7",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "playwright test",
    "check": "tsc --noEmit && vitest run",
    "format": "prettier --write src tests scripts *.json *.ts README.md docs/*.md"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/chrome": "^0.1.24",
    "@types/jsdom": "^30.0.0",
    "@types/node": "^22.18.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
